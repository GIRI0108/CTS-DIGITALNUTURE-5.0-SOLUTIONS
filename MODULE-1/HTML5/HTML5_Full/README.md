HTML5 Full Tasks

Each task folder has a single index.html file.
CSS is written inside the same HTML file using style tag.
JavaScript is written inside the same HTML file using script tag.
Images given by the user are added inside images folders.
Open index.html in Chrome to test each task.
